import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#603A0B',
    },
    secondary: {
      main: '#CDDEF9',
    },
    background: {
      default: '#CDDEF9',
    },
  },
});

export default theme;
